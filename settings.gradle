rootProject.name = 'benchmarks'

include (
    'benchmarks-api',
    'benchmarks-aeron',
    'benchmarks-grpc',
    'benchmarks-kafka',
    'benchmarks-all')

project(':benchmarks-all').projectDir.mkdirs()
